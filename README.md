# Black-Box
This project implements a Black Box Adversarial Attack on an image classification model using the MNIST dataset. It demonstrates how adversarial inputs can manipulate model predictions without access to internal parameters. The project includes model training, attack generation, evaluation, and result visualization.
